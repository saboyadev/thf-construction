---
import MobileMenu from "../components/MobileMenu.astro";
import TopBar from "../components/TopBar.astro";
---

<!-- Kept outside [data-nav] so it scrolls away and never joins the fixed nav -->
<TopBar />
<div data-nav-placeholder>
  <nav
    data-nav
    class="relative left-0 top-0 z-50 w-full border-b border-rule bg-white transition-transform duration-300 ease-out"
  >
    <!-- Inner Nav Container -->
    <div class="mx-auto max-w-[1280px] px-5 sm:px-6 lg:px-8">
      <slot />
    </div>
    <MobileMenu />
  </nav>
</div>
